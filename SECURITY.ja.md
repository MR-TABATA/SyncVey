# セキュリティポリシー

English version: [SECURITY.md](SECURITY.md)

## 脆弱性の報告

**セキュリティ上の問題は公開 Issue に書かないでください。**

GitHub の非公開脆弱性報告をお使いください:

> **[脆弱性を報告する](https://github.com/MR-TABATA/SyncVey/security/advisories/new)**

（リポジトリ → **Security** タブ → **Report a vulnerability**）

この経路は報告者とメンテナの間だけで閉じているので、修正が用意できるまで
内容が公開されません。

書いていただきたいこと:

- 何が問題で、攻撃者が何を得られるのか
- 再現手順（長い手順より、最小の再現例のほうが有用です）
- 確認したバージョンまたはコミット
- 既に分かっている回避策があれば

個人で保守しているプロジェクトなので、対応は契約ではなくベストエフォートです。
最初の返信までおおむね1週間を見てください。それを過ぎても反応が無い場合は、
アドバイザリのスレッドで催促していただいて構いません。

## このツールが保持するもの

SyncVey はセルフホストです。つまり**データを持つのは運用者であって、
プロジェクト側ではありません**。ホスティングサービスもテレメトリも無く、
既定では何もどこへも送信しません。ただし、デプロイされた環境が何を抱えるかは
報告の重要度を判断する材料になります:

| 保持するもの | なぜ重要か |
| --- | --- |
| AWS 資格情報またはロール ARN | 設計上は読み取り専用（[`iam/iam-policy.json`](iam/iam-policy.json)）だが、実在の AWS アカウントに届く |
| スキャンしたリソース情報 | インスタンス ID・ARN・エンドポイント・タグ — 運用者のインフラ構成そのもの |
| 取り込んだ tfstate | アップロード時に機微な値を検査し、取込前に運用者へ警告する |
| ユーザーアカウントと TOTP シークレット | ダッシュボードの認証情報 |

上記を露出させるもの、とりわけ**組織をまたぐアクセス**（マルチテナントのため）
が、最も優先度の高い報告対象です。

## 外部への通信

既定で通信する相手は AWS だけです。それ以外はすべてオプトインで、README の
「外部通信」の表に列挙してあります（Slack Webhook、`endoflife.date` の更新、
CloudTrail による変更者特定）。**その表に無い宛先へ接続する挙動があれば、
それ自体がバグ**なので報告に値します。

## 安全に配置するために

既定値はデモではなく実運用を前提にしています:

- `DEBUG=False` では `SECRET_KEY` が必須（未設定なら起動を拒否します）
- `DEBUG=False` ではセッション／CSRF Cookie が既定で `Secure`
- 本番では `ALLOWED_HOSTS` を明示的に設定してください

`DEBUG=True` はローカル開発専用です。到達可能なホストで有効にすると
スタックトレースや設定が露出します。

## サポート対象バージョン

修正は最新リリースにのみ入ります。保守対象のリリースブランチはありません。
